{
  "name": "astris-web",
  "version": "0.2.0",
  "private": true,
  "description": "ASTRIS — Advanced Satellite Tracking & Real-time Interactive System",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n SERVER,CLIENT -c blue,green \"npm run dev --workspace astris-server\" \"npm run dev --workspace astris-client\"",
    "server": "npm run dev --workspace astris-server",
    "client": "npm run dev --workspace astris-client",
    "build": "npm run build --workspace astris-client",
    "test": "npm run test --workspace astris-server",
    "check": "npm run check:map-parity && npm run test && npm run build",
    "bench:scene": "npm run bench:scene --workspace astris-server",
    "check:render-budget": "npm run check:render-budget --workspace astris-server",
    "check:map-parity": "node tools/validation/current/validate-v020.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=22.13.0"
  }
}
